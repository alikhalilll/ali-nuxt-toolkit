# Media

Drop `hero.png` here (≈1200 × 720, ~150–250 KB) — a screenshot of `<ATelInput>` with the country picker open on the light theme. Referenced from the package README via `raw.githubusercontent.com`.
